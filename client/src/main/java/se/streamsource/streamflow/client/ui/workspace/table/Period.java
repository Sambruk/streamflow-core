package se.streamsource.streamflow.client.ui.workspace.table;

/**
 * Contains enumeration of common time periods.
 */
public enum Period
{
   none,
   one_day,
   three_days,
   one_week,
   two_weeks,
   one_month,
   six_months,
   one_year
}
